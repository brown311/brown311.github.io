// @ts-check
import { defineConfig } from 'astro/config';

// TODO: replace "username" with your actual GitHub username before deploying
// (repo must be named exactly `username.github.io` for this URL to be correct)
// https://astro.build/config
export default defineConfig({
	site: 'https://username.github.io',
});
